name: Cron Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  cron_test:
    runs-on: ubuntu-latest
    steps:
      - name: Cron ok
        run: |
          date
          echo "Cron OK"
